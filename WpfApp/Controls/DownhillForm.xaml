<UserControl x:Class="WpfApp.Controls.DownhillForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Rectangle Grid.ColumnSpan="2" Grid.Row="0" Fill="#FF282828"/>
        <TextBlock Grid.Column="0" 
                   Grid.Row="0" 
                   Margin="10 10 10 10" 
                   Grid.ColumnSpan="2" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontWeight="Bold">DOWNHILL SEGMENT</TextBlock>

        <TextBlock Grid.Column="0" Grid.Row="1" Margin="10 10 0 10">Distance (km)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="10" Text="{Binding DownhillFormViewModel.DistanceForm, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>

        <TextBlock Grid.Column="0" Grid.Row="2" Margin="10 10 0 10">Expected Average Speed (km/h)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="10" Text="{Binding DownhillFormViewModel.TargetedAverageSpeedForm, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>

        <Button 
            Grid.Column="0" 
            Grid.Row="4"
            Margin="10"
            Padding="10 5 10 5"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Command="{Binding AddDHSegementToRideCommand, Mode=OneWay}">Add DH Segment</Button>

        <TextBlock Grid.Row="4" Grid.Column="1" FontWeight="Bold" Margin ="10" VerticalAlignment="Center" 
                   Text="{Binding DownhillFormViewModel.EstimatedTimeTextBlock, Mode=OneWay}"/>

    </Grid>
</UserControl>
